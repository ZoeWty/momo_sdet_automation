[pytest]
testpaths = tests
addopts = -ra --strict-markers --browser chromium --tracing retain-on-failure --screenshot only-on-failure --output test-results
markers =
    smoke: P0 search journeys that must pass before submission

